# Command: `maint` (Deprecated)

This command group has been deprecated in v2.0 in favor of Noun-based operations.

*   `maint dedupe` -> See `collection duplicates`
*   `maint pdfs` -> See `item pdf`
*   `maint migrate` -> See `review migrate`
*   `maint sync-csv` -> See `review sync-csv`
