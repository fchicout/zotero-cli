# Command: `system info`

Display environment information and the current active configuration.

**Usage:**
```bash
zotero-cli system info
```

This command shows:
*   The path to the configuration file being used (if any).
*   The target Library ID and Library Type (User/Group).
*   Whether the API Key is correctly detected from environment variables or config.
