[project]
name = "paper2zotero"
version = "0.1.0"
description = "A tool to add papers from various sources (arXiv, BibTeX) to Zotero"
authors = [{name = "Fábio Chicout", email = "fchicout@gmail.com"}]
requires-python = ">=3.8"
dependencies = [
    "requests",
    "beautifulsoup4",
    "feedparser",
    "bibtexparser",
    "arxiv"
]
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

